export default class Book {
    constructor(title, pages, synopsys, review, image, user) {
        this.title = title
        this.pages = pages
        this.synopsys = synopsys
        this.review = review
        this.image = image
        this.user = user
    }
}